format_version: 10
pipelines:
  ci-server:
    group: tools
    materials:
      git-683112d:
        git: https://github.com/a78devops/tools-automation-code.git
        shallow_clone: false
        auto_update: true
        branch: main
    secure_variables:
      vault_token: AES:jSwi//hkavP/9+1oPVET8A==:84UyWfMFnz4ODf1l2oHSgoIcQvzAEBVFz99Zp9sZyRs=
    stages:
      - ansible:
          fetch_materials: true
          approval:
            type: manual
          jobs:
            ansible:
              tasks:
               - exec: {command: ansible-playbook, arguments: [ "-i", "ci-server-internal.bdevops55.online,", "tools.yml", "-e", "tool_name=ci-server", "-e", "ansible_user=ec2-user", "-e", "ansible_password=DevOps321" "-e", "vault_token=${vault_token}"  ], run_if: passed}

